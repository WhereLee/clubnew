apiVersion: 1

providers:
  - name: 'club-flow-dashboards'
    orgId: 1
    folder: 'Club Flow'
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
